/****************************************************************************
说明：本程序测试指定结构体是否有字节对齐效应，即指定结构体的实际占用空间是不是
	与结构体定义的空间一样
测试结论：测试用的结构体不存在字节对齐效应
*****************************************************************************/
#include <stdio.h>
struct info{
	char a[2];
	char b[2];
	char c[2];
	char d;
};
void main()
{
	struct info info1;
	printf("size of struct is %d\n",sizeof(info1));
}