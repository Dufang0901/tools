关于input子系统：
1、在应用层通过uinput创建设备后，为什么在/dev/input下会出现两个设备节点（event0,mouse0）？
2、以上这两个设备节点有什么关系，作用分别是什么？
3、在input子系统框架下编写输入设备驱动时，只是创建和注册了输入设备，没有去注册、创建驱动设备节点，
	最好是能够跟读内核代码，看下驱动设备节点是怎么创建的？对于一个设备会创建几个设备节点？是根据
	什么决定创建这几个设备节点的？

关于QT的ts输入：
1、tslib的作用，如何使用？
2、如果不用tslib，QT应该使用哪个设备节点作为输入设备？（即环境变量QWS_MOUSE_PROTO应该设置成什么？）